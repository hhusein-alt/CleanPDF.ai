# FastAPI framework
fastapi

# ASGI server
uvicorn[standard]

# File uploads
python-multipart
aiofiles

# Templating
jinja2

# PDF processing
PyMuPDF

# Security / filename sanitization
Werkzeug

# Optional: PDF compression & conversion
pikepdf          # для оптимизации и сжатия PDF
reportlab        # для генерации PDF, если понадобится

# Optional: Environment variables
python-dotenv

# Optional: Logging enhancements
loguru
